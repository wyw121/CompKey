# 《数据分析与商务智能》竞争性关键词推荐项目实验方案设计（仅计划，不执行）

> 版本：v1.0  
> 日期：2026-04-14  
> 适用对象：课程项目小组（建议4人）  
> 说明：本文档为**实施方案与证据留痕设计**，当前不执行实验，仅定义后续全过程的可操作流程、验收口径与取证规范。

---

## 1. 项目目标与约束

### 1.1 总目标

围绕课程四个阶段，完成从数据处理、CompKey算法实现、系统设计到系统开发的全链路实践，并形成可验收的成果包：

- 算法正确性可验证
- 感知竞争性评估可统计复现
- 效率分析可图表化
- 系统功能可演示与可追溯

### 1.2 当前已知数据基础（基于附件目录分析）

当前目录：`数据分析与商务智能数据/`

- `数据/`
  - `user_tag_query.10W.TRAIN`
  - `user_tag_query.10W.TEST`
  - `数据说明.txt`
- `搜索日志/`
  - `SogouQ.reduced.zip`
  - `SogouQ.zip`
  - `数据说明.txt`

**分析结论（用于后续实验设计）：**

1. 存在用户画像+查询列表类数据（年龄、性别、教育、查询列表等字段说明）。
2. 存在搜索日志类数据（字段说明含时间、用户ID、查询词、URL信息等）。
3. 文档读取存在疑似编码问题（乱码风险），需在阶段一制定统一编码策略并留证。

---

## 2. 全流程证据留痕规范（核心要求）

你提出“每一步都要可截图/可保留证据”，本项目统一采用“**三层证据**”：

1. **过程证据（截图）**：IDE界面、终端运行、数据库表、问卷后台、图表页面。
2. **结果证据（文件）**：CSV/JSON/SQL/日志/图表PNG/报告MD。
3. **可复现实证（命令与参数记录）**：关键运行参数、数据版本、样本规模、时间戳。

### 2.1 证据文件夹规范（建议）

在项目根目录建立：

- `evidence/phase1/`
- `evidence/phase2/`
- `evidence/phase3/`
- `evidence/phase4/`
- `evidence/common/`（团队分工、会议纪要、版本清单）

### 2.2 截图命名规范（强制）

命名格式：`阶段-步骤-时间-操作者-说明.png`  
示例：`P2-S5-20260418-zhangsan-competition_score_distribution.png`

### 2.3 结果文件命名规范（强制）

命名格式：`阶段_对象_版本_日期.ext`  
示例：`P1_seed_related_queries_v1_20260416.csv`

### 2.4 每步留痕最小集（Minimum Evidence Set）

每个关键步骤至少保留：

- 1张操作截图
- 1个输出结果文件
- 1条文字记录（输入、参数、输出规模、异常说明）

---

## 3. 团队角色与分工（4人模板）

- **组长/项目经理（A）**：进度推进、验收对齐、汇总答辩材料。
- **数据工程（B）**：阶段一数据处理、编码修复、分词与数据资产输出。
- **算法工程（C）**：阶段二CompKey实现、性能测试、统计图表。
- **系统工程（D）**：阶段三/四系统设计、数据库与界面开发、集成演示。

> 每位成员必须在 `evidence/common/worklog.md` 中记录个人工作量（日期、时长、任务、产出链接）。

---

## 4. 四阶段详细实施计划（含证据点）

## 4.1 第一阶段：数据采集与处理

### 4.1.1 目标

完成数据预处理并提取与10–20个种子关键词相关的数据，形成可直接输入算法的数据集。

### 4.1.2 步骤与证据

#### 步骤P1-1：确定种子关键词（10–20个）

- 操作：小组讨论业务场景（如教育、3C、美妆等），生成种子词表。
- 输出：`seed_keywords_v1.csv`（字段：keyword, domain, reason, owner）。
- 截图：词表文件与讨论记录截图。
- 验收点：关键词数量在10~20，且每个词有选择理由。

#### 步骤P1-2：原始数据读取与编码检查

- 操作：读取 `user_tag_query.10W.TRAIN/TEST` 与搜索日志样本，检查乱码与编码。
- 输出：`encoding_check_report.md`（文件编码、异常行数、处理策略）。
- 截图：读取前后对比截图（乱码→正常）。
- 验收点：明确统一编码方案（建议优先尝试GBK/UTF-8并留日志）。

#### 步骤P1-3：数据清洗与去重

- 操作：清理空查询、重复记录、非法字符。
- 输出：`cleaned_queries_v1.csv` + `cleaning_log.json`。
- 截图：清洗前后数据量对比表。
- 验收点：清洗规则可复述，可重跑。

#### 步骤P1-4：基于种子词检索相关数据

- 操作：从清洗后数据中筛出含种子词或相关上下文的记录。
- 输出：`seed_related_queries_v1.csv`。
- 截图：检索条件与命中结果条数截图。
- 验收点：每个种子词都有命中统计。

#### 步骤P1-5：分词与中间结构化保存

- 操作：对筛选数据分词，生成词频、共现基础表。
- 输出：`tokenized_queries_v1.csv`、`word_freq_v1.csv`。
- 截图：分词结果样例与Top词频图。
- 验收点：分词结果可用于阶段二中介词提取。

#### 步骤P1-6：阶段一验收包整理

- 操作：汇总代码、数据、日志、截图、说明。
- 输出：`P1_delivery_checklist.md`。
- 截图：阶段一成果目录截图。
- 验收点：满足“预处理完成并可验收”。

### 4.1.3 第一阶段思考题答题证据

- 文件：`P1_thinking_answers.md`
- 需包含：
  1. 大数据量下快速搜索策略（如倒排、分块、流式处理）
  2. 编码乱码解决方案与验证证据
  3. 分词方法横向对比（准确率、速度、可解释性）

---

## 4.2 第二阶段：CompKey算法设计与实现

### 4.2.1 目标

完成CompKey竞争性关键词推荐算法实现、感知竞争性评估、效率分析。

### 4.2.2 算法流程（计划态）

种子词 $s$ → 提取中介词集合 $M(s)$ → 统计联合查询量/共现量 → 计算中介权重 $w_m$ → 形成候选竞争词集合 $C(s)$ → 计算竞争度 $Score(c|s)$ → 排序输出Top-N。

> 注：具体公式在实现前于 `algorithm_spec.md` 中冻结版本，避免后期口径漂移。

### 4.2.3 步骤与证据

#### 步骤P2-1：中介关键词提取

- 输出：`mediator_keywords_v1.csv`（seed, mediator, freq, support）。
- 截图：提取脚本运行与结果预览。
- 验收点：每个seed均有中介词集合。

#### 步骤P2-2：搜索量/共现统计

- 输出：`cooccurrence_stats_v1.csv`。
- 截图：统计结果摘要（行数、均值、最大值）。
- 验收点：统计口径固定且有字段说明。

#### 步骤P2-3：权重计算实现

- 输出：`mediator_weight_v1.csv` + `weight_formula_note.md`。
- 截图：权重计算样例（手工核验1~2条）。
- 验收点：可解释、可复算。

#### 步骤P2-4：候选竞争词提取

- 输出：`candidate_competitive_keywords_v1.csv`。
- 截图：候选词来源与过滤条件截图。
- 验收点：候选集合具备业务可解释性。

#### 步骤P2-5：竞争度计算

- 输出：`competitive_score_v1.csv`。
- 截图：单个seed的竞争度分布图。
- 验收点：计算稳定，异常值可说明。

#### 步骤P2-6：排序输出推荐结果

- 输出：`compkey_topn_result_v1.csv`。
- 截图：Top-N结果页（按seed分组）。
- 验收点：排序规则明确、输出可复现。

#### 步骤P2-7：感知竞争性问卷评估（20–30份）

- 输出：
  - `questionnaire_template_v1.docx`
  - `questionnaire_raw_data_v1.xlsx`
  - `questionnaire_analysis_v1.md`
- 截图：问卷设计页面、回收数量、统计图（均值/方差/一致性）。
- 验收点：样本量满足要求，结论与算法结果可对照。

#### 步骤P2-8：算法时间效率分析

- 输出：`runtime_benchmark_v1.csv` + `runtime_plot_v1.png`。
- 截图：不同数据量运行时间曲线。
- 验收点：结论能回答“数据量与耗时关系”。

### 4.2.4 第二阶段思考题答题证据

- 文件：`P2_thinking_answers.md`
- 需包含：
  1. 完整CompKey流程图与复杂度影响因素
  2. 程序设计合理性、缺陷与优化方向

---

## 4.3 第三阶段：系统设计与开发（上）

### 4.3.1 目标

完成系统需求分析、概要设计、模块设计、数据库设计与落库验证。

### 4.3.2 步骤与证据

#### 步骤P3-1：需求分析

- 输出：`SRS_v1.md`（角色、用例、非功能需求）。
- 截图：用例图/需求清单截图。
- 验收点：需求与课程目标一一对应。

#### 步骤P3-2：系统架构设计

- 输出：`architecture_design_v1.md`（前后端/算法服务/数据层）。
- 截图：架构图。
- 验收点：模块边界清晰、调用关系明确。

#### 步骤P3-3：功能模块设计

- 输出：`module_design_v1.md`（输入、处理、输出、异常）。
- 截图：模块时序图/流程图。
- 验收点：覆盖关键字输入、推荐输出、数据库更新。

#### 步骤P3-4：数据库设计与创建

- 输出：`db_schema_v1.sql` + `db_dictionary_v1.xlsx`。
- 截图：建表成功、关键SQL查询结果。
- 验收点：满足“数据库创建与操作并验收”。

### 4.3.3 第三阶段思考题答题证据

- 文件：`P3_thinking_answers.md`
- 需包含：
  1. 应用场景与服务目标
  2. 不同推荐系统比较与适用场景

---

## 4.4 第四阶段：系统设计与开发（下）

### 4.4.1 目标

实现系统全部功能模块与用户界面，形成可演示原型。

### 4.4.2 步骤与证据

#### 步骤P4-1：关键字输入功能

- 输出：`ui_keyword_input_demo.mp4`（可选）+ 输入校验截图。
- 截图：输入、校验提示、提交成功。
- 验收点：输入流程与阶段一处理逻辑一致。

#### 步骤P4-2：推荐结果输出功能

- 输出：`ui_recommendation_result_sample.png` + `api_result_sample.json`。
- 截图：推荐列表、竞争度、排序字段。
- 验收点：结果与阶段二算法输出一致。

#### 步骤P4-3：数据库更新功能

- 输出：`db_update_log_v1.csv`。
- 截图：写入前后对比、更新成功记录。
- 验收点：新增/更新逻辑正确，字段完整。

#### 步骤P4-4：界面设计与可用性改进

- 输出：`ui_design_notes_v1.md`。
- 截图：主要页面全景+关键交互动线。
- 验收点：可完成一条完整业务链（输入→推荐→落库）。

### 4.4.3 第四阶段思考题答题证据

- 文件：`P4_thinking_answers.md`
- 需包含：
  1. 系统设计理念说明
  2. 与现有系统对比及下一步优化方向

---

## 5. 里程碑与时间安排（建议）

- 第1周：阶段一（数据处理）
- 第2~3周：阶段二（算法+问卷+效率）
- 第4周：阶段三（需求/架构/数据库）
- 第5周：阶段四（实现/联调/演示）
- 第6周：总验收资料整理与答辩彩排

> 每周固定一次组会，输出 `evidence/common/weekly_minutes_YYYYMMDD.md`。

---

## 6. 评分导向的交付清单（验收即对照）

### 6.1 必交物（按阶段）

1. 阶段一：预处理数据成果 + 验收证据
2. 阶段二：CompKey实现 + 问卷统计 + 时间效率图表
3. 阶段三：系统设计文档 + 数据库创建与操作证据
4. 阶段四：系统实现 + UI演示 + 个人工作量记录

### 6.2 统一总交付目录（建议）

- `deliverables/P1/`
- `deliverables/P2/`
- `deliverables/P3/`
- `deliverables/P4/`
- `deliverables/final_report.md`
- `deliverables/presentation.pptx`

---

## 7. 风险与预案（重点）

1. **编码乱码风险**：采用“读取探测+统一转码+抽样核验”三步法，并保留前后对照证据。
2. **数据量过大导致性能瓶颈**：先做小样本验证，再分层扩大数据规模，记录耗时曲线。
3. **问卷样本不足**：提前准备回收渠道，设置回收进度看板。
4. **算法口径漂移**：公式与字段定义先冻结（`algorithm_spec.md`），后续改动必须记录版本。
5. **团队协作断档**：统一Git分支命名、提交规范、周报机制。

---

## 8. 附：可直接执行的“每步证据记录模板”

复制为 `evidence/common/step_record_template.md` 使用：

- 步骤编号：
- 目标：
- 输入数据与版本：
- 执行人：
- 执行时间：
- 操作简述：
- 参数/配置：
- 输出文件路径：
- 截图路径：
- 结果摘要（数量、质量、异常）：
- 是否通过验收（是/否）：
- 复现说明：

---

## 9. 本文档与课程要求对齐结论

本文档已覆盖课程指导书四阶段全部要求，并将每个关键动作映射为“**步骤-产出-截图-验收点**”结构，满足你提出的“后续每一步都有可截图或可保留关键内容，且能展示完成证据”的目标。

下一步（你确认后）可进入：

1. 先落地阶段一的目录与模板；
2. 再按本文档逐步执行并实时累计证据。
